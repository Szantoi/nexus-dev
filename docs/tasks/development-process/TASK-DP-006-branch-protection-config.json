{
  "_provenance": {
    "task_id": "TASK-DP-006",
    "purpose": "Draft GitHub branch protection payload for `main`. NOT APPLIED — proposed only, awaiting human approval (see ADR-086, section 'Nyitott kérdések'). The `_provenance` key is metadata for humans reading this file and MUST be stripped before use as an API request body — send only the `protection_payload` object.",
    "target_endpoint": "PUT /repos/Szantoi/nexus-dev/branches/main/protection",
    "example_apply_command": "gh api --method PUT repos/Szantoi/nexus-dev/branches/main/protection --input <(node -e \"process.stdout.write(JSON.stringify(require('./TASK-DP-006-branch-protection-config.json').protection_payload))\")  -- NOT RUN by TASK-DP-006, documented only",
    "generated_at": "2026-07-18",
    "read_only_current_state_check": {
      "command": "gh api repos/Szantoi/nexus-dev/branches/main/protection",
      "result": "404 Branch not protected (verified 2026-07-18, read-only, no write performed)"
    },
    "known_caveat": "Updated by TASK-DP-007 (2026-07-27): ci.yml now runs the `knowledge-service` job as an OS matrix, so the check-run names are 'knowledge-service (ubuntu-latest)' and 'knowledge-service (windows-latest)' — both listed as required contexts below. Re-verify the exact names against the GitHub Checks tab of a fresh run before applying."
  },
  "protection_payload": {
    "required_status_checks": {
      "strict": true,
      "checks": [
        { "context": "knowledge-service (ubuntu-latest)" },
        { "context": "knowledge-service (windows-latest)" }
      ]
    },
    "enforce_admins": true,
    "required_pull_request_reviews": {
      "required_approving_review_count": 1,
      "dismiss_stale_reviews": true,
      "require_code_owner_reviews": false,
      "require_last_push_approval": true
    },
    "restrictions": null,
    "required_linear_history": false,
    "allow_force_pushes": false,
    "allow_deletions": false,
    "required_conversation_resolution": true,
    "lock_branch": false,
    "allow_fork_syncing": true
  }
}
